image:
  repository: public.ecr.aws/aws-containers/retail-store-sample-orders
  pullPolicy: IfNotPresent
  tag: 1.3.0

postgresql:
  create: false

rabbitmq:
  create: false

app:
  persistence:
    provider: postgres
    endpoint: "orders-postgres-db.cmfy84y8mlfq.us-east-1.rds.amazonaws.com:5432"   # from terraform output
    database: ordersdb
    secret:
      create: true
      name: orders-db
      username: dbadmin
      password: "0hEyfUAVRG4xQbBu"
  messaging:
    provider: sqs
    sqs:
      queueName: "retail-dev-orders-queue"                    # from terraform output

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 512Mi